# 德语学习网站整改与完成度报告

检查日期：2026-08-09
仓库：<https://github.com/Mattrissimo/deutsch-lernen>  
线上版本：<https://mattrissimo.github.io/deutsch-lernen/>

## 2026-08-09 新教材路线验收结果

旧的 60 集主题路线已替换为《新编大学德语》第 1、2 册：20 个单元 × 3 个学习步骤，共 60 小节。正式课程入口与公开备选视频分开显示，网站不再把目录摘要描述成视频逐字内容。

本地自动验收结果：

| 项目 | 结果 |
|---|---:|
| 教材单元与学习步骤 | 20 单元 / 60 小节 |
| 备用视频映射 | 40 / 40 课时 |
| 教材核心词 | 240 / 240 |
| 全课程名词词形 | 239 个；0 个漏定冠词；0 个“未收录”复数 |
| 单元考试 | 20 / 20 单元；每单元 20 题 |
| 结构化格数题 | 87 道；0 个缺解析 |
| 例句补充词 | 当前课 0 个遗漏；全部进入词卡/闯关逻辑 |
| 手机答题 | 390×844 与 360×640 均无跳页、刷新、遮挡或横向溢出 |
| 全站技术自检 | 全部通过；0 个页面脚本错误 |

教材中的练习不以整页扫描图公开发布，而是按本单元课文、语法与词汇重做成选择、填空、格数、变位和店铺场景题；每题标注教材册次和单元，并提供三级提示、答案解析和语音。这样既能直接作答，也不会把用户拥有的整本教材扫描件公开转载到 GitHub Pages。

## 一、结论

V2 已于 2026-07-21 发布到 GitHub 仓库 `main`，网页、SQL、发布说明和本报告与本地校验版本完全一致；GitHub Pages 最终构建与部署成功。别人现在打开公开网址会进入整改后的 V2。

基础学习、单词、复习、课程入口和练习已经上线；最新 Supabase SQL 与正式跳转 URL 已配置。完整字母与重点拼读规则已接入公开母语者真人录音。两个 Supabase Edge Function 已部署：AI 老师被固定为 Gemini 免费优先，未配置免费 key 时只使用离线老师；付费 TTS 默认关闭。完整账号云进度还需要双账号、跨设备实际验收；新教材路线已完成目录、主题和练习对齐，但仍不等同视频逐字讲义。

## 二、账号和跨设备进度是否独立

### 整改后的设计

| 使用方式 | 数据结果 |
|---|---|
| 不登录（访客） | 只保存在当前浏览器，和登录账号分开；换设备不会同步。 |
| 同一个账号登录电脑、iPhone、iPad | 合并同一账号的生词、复习、课程完成、DW/视频完成、闯关、今日计划和已掌握词。 |
| 不同账号登录同一台设备 | 使用不同的本机存储命名空间，并读取数据库中各自的 `user_id` 行，进度彼此独立。 |
| 不同账号登录不同设备 | 数据库按 `user_id` 分行，进度彼此独立。 |

数据库脚本已启用并强制 Row Level Security，只允许登录用户读取、插入和更新 `auth.uid() = user_id` 的行。浏览器端也不再让所有账号共用一套无账号前缀的学习状态。

为了不丢失旧版数据，旧版浏览器里的未分账号数据只允许第一个登录账号认领一次，并在首次同步时与该账号已有云数据做一次无损合并；后续账号不会再自动合并这份旧数据。

### 必须说明的验证边界

代码和 SQL 策略已经完成静态核对；生产接口检查已从旧版的“`progress` 字段不存在”变为匿名访问 401 受保护，符合新 SQL 添加字段并撤销匿名表权限后的预期结果。仍需按发布说明用两个真实邮箱交叉登录验收，才能把生产环境标记为“已验证独立”。

当前 GitHub 线上代码已经使用账号作用域存储和完整进度同步，SQL 与 Auth URL 也已配置；但在双账号尚未实测前，**生产环境仍不能标记为“已验证独立”**。

## 三、本轮性能与稳定性整改

- 单词库由一次渲染全部卡片改为每批 24 张，搜索增加短延迟合并输入，减少手机滚动和频繁重绘。
- 生词列表按批显示，新加生词仍置顶；不再首次打开就自动塞入 240 个课程词。
- 教材正式课程与 40 课时公开备选视频分开：正式课程需要登录，备选视频只跳转或嵌入；网络受限时教材文字、练习和本机进度仍可工作。
- 不再运行时搜索未知的词典录音；发音课改用已核实文件名的固定 Wikimedia 母语者录音，一次点击只加载一段音频，并为每段录音显示来源与许可。
- 全站语音默认零费用：固定字母/规则优先使用公开真人录音，任意文本和缺失录音使用设备德语语音；付费 TTS 后端默认拒绝请求，只有管理员显式开启才可能产生费用。
- 单词卡点击改为容器级统一处理，不再在每批卡片重画后重复创建多组监听器；搜索文本建立内存缓存，切换“今天学习”不再触发重复云同步和首页重画。
- 在线老师保留最近 8 条对话上下文，提交期间锁定按钮并使切换场景时的旧请求失效；后端缓存 10 分钟内的完全相同请求，减少重复额度消耗。
- 登录状态切换增加重复事件抑制，避免页面初始化时同一账号重复同步和重复重绘。
- 登录入口移到页面最上方，并始终明确显示当前是访客进度还是某个邮箱账号的独立云进度；生词区不再重复放置第二套登录框。
- 学习状态写入改为账号作用域，并用 700ms 合并云同步请求。
- 修复“删除生词或取消完成后又被云端合并回来”的问题：已有时间信息时以最近修改的一端为准，只有旧数据首次迁移时才做无损合并。
- 修复同步进行中再次修改时可能漏掉最后一次写入的问题；进行中的修改会排队，在当前请求结束后再次同步。
- 切换或退出账号时会取消旧账号尚未开始的同步；已发出的旧请求也会在写入本机状态前再次核对账号，避免跨账号污染。
- JavaScript 已通过语法检查；HTML 中未发现损坏的 UTF-8 替换字符。

本轮还用 Windows Edge 的 390 × 844 手机视口做了页面运行测试：初始化无 JavaScript 页面错误，整页无横向溢出，单词库首批为 24 张，96 条已渲染例句全部常驻可见，快速加入 `Vokabeln` 能自动补义并置顶，上一课/下一课切换正常。浏览器内模拟账号 A/B 后，两套作用域存储分别保持 A/B 数据，没有互相覆盖。

## 四、《新编大学德语》课程内容核对

已核对用户提供的《新编大学德语》第 1、2 册目录，共 20 个单元；网站按每单元 3 个步骤组织为 60 个学习小节。正式课程来自国家高等教育智慧教育平台，公开备选列表共 40 课时，按每单元 2 课时映射。

页面已有 60 个教材步骤、每步 4 个核心词，共 240 个教材词条；课文、听力、阅读和语法主题按册次与单元显示。每单元另有 20 题综合考试，教材练习题型已改造成可判题的互动练习。

在没有合法字幕、讲义或逐课人工听记的情况下，不能可靠补出老师视频中的每个例子、板书和补充说明。页面会明确区分“教材已核对内容”和“视频公开备选入口”，不再伪装成逐字对齐。

后续若取得合法字幕或教师讲义，再按 20 个单元逐段补充时间戳、教师例句、板书和纠错，不靠标题自动猜测。

## 五、别人现在能否使用全部功能

| 功能 | 本地 V2 | 当前线上 V2 | 条件或缺口 |
|---|---:|---:|---|
| 单文件页面、手机/电脑打开 | 可用 | 可用 | GitHub Pages 或直接打开 HTML。 |
| 今日一条龙、分类入口、听写闯关 | 可用 | 可用 | 已上线。 |
| 生词、艾宾浩斯复习、统计 | 可用 | 可用 | 访客只保存在本机。 |
| 不同账号独立完整进度 | 代码与数据库已配置 | 待生产验收 | 使用两个真实账号交叉登录验收。 |
| 同账号跨设备完整同步 | 代码与数据库已配置 | 待生产验收 | 使用同一账号在第二台设备登录验收。 |
| 《新编大学德语》20 单元 / 60 学习步骤 | 可用 | 待本轮发布 | 正式课程需登录；公开备选 40 课时；不是老师逐字讲义。 |
| 完整字母与重点拼读规则 | 可用 | 可用 | 30 个字母、18 组规则；固定示范使用公开母语者真人录音。 |
| 每句独立德语朗读 | 可用 | 可用 | 零付费模式使用公开真人录音或设备德语语音；`german-tts` 已部署但默认禁用付费生成。动态句子不能假称真人预录。 |
| 真人发音评分 | 未完成 | 未完成 | 需要录音权限、语音识别/发音评分后端和隐私说明。 |
| 在线 AI 德语老师 | 免费额度优先后端已完成 | 后端已部署，待配置 Gemini key | 登录后自动连接 `german-teacher`；当前明确处于离线兜底，不会调用现有 OpenAI key；网页不存接口、令牌或长期模型密钥。 |
| 离线小老师与网站评估 | 可用 | 可用 | 是规则型反馈，不等同真人教师或在线大模型。 |

所以答案是：**别人现在可以使用已经上线的学习功能，账号云同步也已经具备运行条件**。本轮发布后，字母和重点规则会有真实母语者录音；在线老师默认采用 Gemini 免费额度，额度不足时自动使用离线老师。任意文本的云端自然语音仍属于可选付费增强，不部署也不影响公开录音和设备语音。要把账号功能正式标记为交付完成，还要做双账号、跨设备验收；完整视频逐字讲义和发音评分仍未完成。

## 六、上线后验收清单

1. 已完成：V2 文件发布到仓库根目录，GitHub Pages 构建成功。
2. 已完成：在 Supabase SQL Editor 执行最新 `supabase-setup.sql`；只读接口检查确认新版字段已存在且匿名访问受保护。
3. 已完成：Supabase Auth 已配置正式 GitHub Pages Site URL 和两条 Redirect URL。
4. 待完成：用账号 A 加入词 A、完成课程 A；用账号 B 加入词 B、完成课程 B；来回退出登录确认不串数据。
5. 待完成：用账号 A 在第二台设备登录，确认课程、生词、复习和闯关都同步。
6. 建议检查：在 iPhone Safari、Android Chrome、Windows Edge 各检查一次朗读声线、键盘、滚动和弹窗。
7. 没有安全 AI 后端时保持离线老师，不要把任何长期 secret key 写进 HTML。
